version: '3.8'

services:
  # MinIO - S3-compatible object storage (for local development)
  minio:
    image: minio/minio:latest
    command: server /data --console-address ":9001"
    ports:
      - "9000:9000"  # S3 API
      - "9001:9001"  # Web UI
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
    volumes:
      - minio_data:/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3

  # LedgerLens API
  api:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - ./data:/app/data
    environment:
      # OpenAI
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      
      # Pinecone
      - PINECONE_API_KEY=${PINECONE_API_KEY}
      - PINECONE_INDEX_NAME=${PINECONE_INDEX_NAME:-ledgerlens}
      - PINECONE_REGION=${PINECONE_REGION:-us-east-1}
      
      # MinIO (local S3)
      - AWS_ACCESS_KEY_ID=minioadmin
      - AWS_SECRET_ACCESS_KEY=minioadmin
      - AWS_REGION=us-east-1
      - AWS_ENDPOINT_URL=http://minio:9000
      - S3_BUCKET_NAME=ledgerlens-documents
      
      # JWT Authentication
      - JWT_SECRET_KEY=${JWT_SECRET_KEY}
      - JWT_EXPIRY_HOURS=${JWT_EXPIRY_HOURS:-24}
      
      # Turso User Database
      - TURSO_DATABASE_URL=${TURSO_DATABASE_URL}
      - TURSO_AUTH_TOKEN=${TURSO_AUTH_TOKEN}
      
      # External APIs (optional)
      - NEWS_API_KEY=${NEWS_API_KEY}
    depends_on:
      - minio
    restart: unless-stopped

volumes:
  minio_data:
    driver: local

# For production with AWS S3, remove MinIO service and update API environment:
# api:
#   environment:
#     - AWS_ACCESS_KEY_ID=${AWS_ACCESS_KEY_ID}
#     - AWS_SECRET_ACCESS_KEY=${AWS_SECRET_ACCESS_KEY}
#     - AWS_REGION=${AWS_REGION}
#     - S3_BUCKET_NAME=${S3_BUCKET_NAME}
#     # Remove AWS_ENDPOINT_URL for production AWS S3